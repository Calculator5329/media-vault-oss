# Agent instructions

Read `CLAUDE.md`. It is the setup contract for any coding agent (Codex, Claude Code, Cursor,
or another) and this file only points at it so the two never drift.

Codex-specific note: `python vault.py` must run in the background while you check the
viewer; start it as a background job and stop it when you are done.
