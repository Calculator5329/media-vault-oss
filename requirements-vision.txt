# Optional: SigLIP 2 image search, video moment search and Qwen3-VL descriptions.
# Install CPU torch first if you have no NVIDIA GPU (see docs/setup.md), then this file.
torch>=2.6,<3
transformers>=4.57,<6
safetensors>=0.4,<1
